# Canonical NFC card base URL (no #d= fragment).
# Product is iOS-only: new bracelet writes use the redmed:// URL scheme.
# Keep AppConfig.medicalCardBaseURL in sync with the active line below.
#
# Active write target (first uncommented non-comment line):
redmed://card
#
# Legacy HTTPS host (older bracelets / in-app decode still accepted):
# legacy:https://maxroot1122.github.io/RedMed/index.html
