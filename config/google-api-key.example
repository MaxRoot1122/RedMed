# Paste your Google Maps Platform API key on the line below (no quotes).
#
# BEFORE enabling (key becomes public on GitHub Pages via config/google-api-key):
#   1. Enable only Geocoding API + Places API.
#   2. Restrict web key by HTTP referrer:
#        https://maxroot1122.github.io/RedMed/*
#      Optional local: http://127.0.0.1:*/*
#   3. Prefer a separate iOS key restricted to bundle ID local.redmed.app.
#   4. Set hard quotas and billing alerts in Google Cloud Console.
#
# Deploy: copy to config/google-api-key (gitignored) locally, or set GitHub
# Actions secret GOOGLE_MAPS_API_KEY for Pages. See SECURITY.md.

YOUR_GOOGLE_MAPS_API_KEY_HERE
