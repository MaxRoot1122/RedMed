# macOS
.DS_Store
# Desktop hub aliases accidentally copied into repo root (real hub is ~/Desktop/RedMed/)
/RedMed Project
/RedMed Simulator.app
/RedMed iPhone.app
/RedMed.app
/RedMed.xcodeproj
/RedMedOs.app
/Open in Cursor.command
/README.txt
build/icon.iconset/
build/DerivedData/
# Local Xcode DerivedData dumps (never commit — ~115MB landed here once by mistake)
build/ios-DerivedData/
build/
.AppleDouble
.LSOverride

# Local AI / editor junk (never commit)
.claude/
.cursor/
!.cursor/environment.json
.vscode/
.idea/
*.swp
*~
# SweetPad / xcode-build-server — machine-local paths
buildServer.json

# Xcode (created once the project is set up per ios/SETUP.md — sources
# themselves live in ios/RedMed/ and stay tracked)
ios/build/
*.xcodeproj/xcuserdata/
*.xcodeproj/project.xcworkspace/xcuserdata/
*.xcworkspace/xcuserdata/
xcuserdata/
DerivedData/
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example
