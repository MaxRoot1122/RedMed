# French (FR) i18n — same workspace

**Rule:** ship in `MaxRoot1122/RedMed` only. No second repo.

## Done
- [x] `STRINGS.fr` full parity with `en`
- [x] Lang cycle EN → ES → FR; browser `fr*` default
- [x] `toLocaleDateString` for `fr`
- [x] CSP + www mirror + `verify-web`
